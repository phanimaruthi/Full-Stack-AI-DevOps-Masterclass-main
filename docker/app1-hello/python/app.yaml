apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-python

spec:
  replicas: 3

  selector:
    matchLabels:
      app: hello-python

  template:
    metadata:
      labels:
        app: hello-python

    spec:
      containers:
        - name: hello-python
          image: maruthi127/hello-python:latest
          ports:
            - containerPort: 3000

          env:
            - name: ENV_VALUE
              value: "kubernetes"

---
apiVersion: v1
kind: Service
metadata:
  name: hello-python-service

spec:
  type: NodePort

  selector:
    app: hello-python

  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30081